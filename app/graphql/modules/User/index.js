
const UserSchema = require('./schema');
const UserResolvers = require('./resolvers');
const UserProvider = require('./provider.ts');

module.exports = {
  UserSchema,
  UserResolvers,
  UserProvider,
};
